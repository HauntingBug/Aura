import Asteria from "./asteria";
interface MomentumOptions {
    url: string;
    apikey: string;
}
interface User {
    username: string;
    username_lower: string;
    password: string;
    reports: number;
    accountId: string;
    email: string;
    discordId: string;
    createdAt: string;
    updatedAt: string;
    mfa: boolean;
}
type UserKey = "username" | "accountId" | "email" | "discordId";
declare class Momentum {
    private url;
    private apikey;
    constructor(options: MomentumOptions);
    getUser(key: UserKey, value: string): Promise<User>;
    updateUser(key: UserKey, value: string, field: string, newValue: string): Promise<User>;
    getProfile(accountId: string, value: string): Promise<any>;
}
export default Momentum;
export { Asteria };
